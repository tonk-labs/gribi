import { expect, test } from 'vitest'
import { Vault } from '../../src/vault'

test('Test getters and setters', async () => {
    // Vault.setEntry("0x01", "test", {
    //     uri: 
    // });
    // expect(Vault.getDataForCommitment("0x01", "test", "2093232")).toStrictEqual({
    //     commitment: "2093232",
    //     slot: 0,
    //     value: "Hello!"
    // });
    // expect(Vault.getEntries("0x01", "test")[0].value).toBe("Hello!");
    // expect(Vault.getDataAtSlot("0x01", "test", 0)!.value).toBe("Hello!");
    // Vault.removeEntry("0x01", "test", {
    //     commitment: "2093232",
    //     slot: 0,
    //     value: "Hello!"
    // });

    // expect(Vault.getDataForCommitment("0x01", "test", "2093232")).toBeUndefined();
    // expect(Vault.getEntries("0x01", "test").length).toBe(0);
})
